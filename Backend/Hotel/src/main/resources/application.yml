spring:
  application:
    name: Hotel
 
  datasource:
       username: root
       password: root
       url: jdbc:mysql://localhost:3306/HCC01
       driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
        